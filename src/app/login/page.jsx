import FormLogin from "@my/components/FormLogin";
import React from "react";

export const metadata = {
  title: "Login | SDGs Desa Sidorejo",
};

const LoginPage = () => {
  return <FormLogin />;
};

export default LoginPage;
